# Create an API key at https://openrouter.ai/keys and paste it here.
OPENROUTER_API_KEY=sk-or-v1-...

# Optional overrides (the web UI writes the model into agent.config.json instead)
# AGENT_MODEL=anthropic/claude-haiku-4.5
# AGENT_MAX_STEPS=20
# AGENT_MAX_COST=1.0
